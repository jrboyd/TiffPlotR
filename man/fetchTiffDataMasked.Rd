% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiff_image_fetch.R
\name{fetchTiffDataMasked}
\alias{fetchTiffDataMasked}
\title{Fetch TIFF signal from mask-covered pixels across all channels}
\usage{
fetchTiffDataMasked(
  tiff_path,
  rect = NULL,
  resolution = NULL,
  max_pixels = 800,
  precalc_max = NULL,
  show_raw = FALSE,
  quantile_norm = 0.999,
  channel_names = NULL,
  selected_channels = NULL,
  mask_points = NULL,
  retain_unmasked = FALSE,
  bit_order = c("msb", "lsb")
)
}
\arguments{
\item{tiff_path}{Path to the TIFF image file.}

\item{rect}{A \linkS4class{TiffRect} object defining the requested region.
If NULL, the full image extent at maximum resolution is used.}

\item{resolution}{Resolution level to read. If NULL, a resolution is selected
automatically to keep dimensions under \code{max_pixels}.}

\item{max_pixels}{Maximum pixel dimension used when \code{resolution = NULL}.}

\item{precalc_max}{Optional data frame with \code{channel}, \code{min_value},
and \code{max_value} for normalization.}

\item{show_raw}{If TRUE, plot raw values. If FALSE (default), plot normalized values.}

\item{quantile_norm}{Quantile used to estimate per-channel max values when
\code{precalc_max} is NULL.}

\item{channel_names}{Optional character vector used to rename channels.}

\item{selected_channels}{Optional subset of channels to keep, as channel indices
or names.}

\item{bit_order}{Bit order to use when decoding OME mask payloads.}
}
\value{
A \linkS4class{TiffPlotDataMasked} object with:
\itemize{
  \item \code{@data}: mask-filtered signal values for all selected channels
  \item \code{@mask_points}: decoded mask points with metadata
  \item \code{@mask_color_mappings}: per-mask color mapping (defaulting to XML FillColor)
}
}
\description{
Reads TIFF data similarly to \code{fetchTiffData()}, then filters the signal
to pixels that overlap decoded OME \code{Mask} annotations. Mask metadata,
including \code{ROI_ID}, \code{Text}, and \code{FillColor}, are preserved.
}
\examples{
\dontrun{
masked <- fetchTiffDataMasked(exampleTiff())
head(masked@data)
head(masked@mask_color_mappings)
}
}
