% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiff_image_fetch.R
\name{collect_channel_quantiles_all_resolutions}
\alias{collect_channel_quantiles_all_resolutions}
\title{Collect channel quantiles across all TIFF resolutions}
\usage{
collect_channel_quantiles_all_resolutions(
  tiff_path,
  probs = sort(c(seq(0, 100)/100, 0.99 + seq(1, 9)/1000))
)
}
\arguments{
\item{tiff_path}{Path to the TIFF image file.}

\item{probs}{Numeric vector of quantiles to compute. Defaults to:
\code{sort(c(seq(0, 100) / 100, 0.99 + seq(1, 9) / 1000))}.}
}
\value{
A data.frame with columns \code{channel}, \code{resolution},
  \code{prob}, and \code{value}.
}
\description{
Reads every available resolution level once and computes the requested
quantiles for each channel, returning a long-format table that can be
inspected before choosing normalization cutoffs.
}
\examples{
\dontrun{
qtab <- collect_channel_quantiles_all_resolutions(exampleTiff())
subset(qtab, channel == 1)
}
}
