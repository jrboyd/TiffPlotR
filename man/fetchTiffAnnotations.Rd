% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ome_annotations.R
\name{fetchTiffAnnotations}
\alias{fetchTiffAnnotations}
\title{Fetch OME ROI annotations from a TIFF}
\usage{
fetchTiffAnnotations(
  tiff_path = NULL,
  ome_doc = NULL,
  decode_masks = FALSE,
  bit_order = c("msb", "lsb"),
  include_summary = TRUE
)
}
\arguments{
\item{tiff_path}{Path to the TIFF image file. Required when `ome_doc` is NULL.}

\item{ome_doc}{Optional xml2 document (as returned by \code{xml2::read_xml}).
Supplying this skips OME-XML loading from disk.}

\item{decode_masks}{If TRUE, decode mask bitmaps to point coordinates.}

\item{bit_order}{Bit order to use when decoding mask payloads.}

\item{include_summary}{If TRUE, include a node-type summary table.}
}
\value{
A named list with elements:
\itemize{
  \item \code{summary}: node-type summary (or NULL when disabled)
  \item \code{roi}: ROI table with \code{ROI_ID}
  \item \code{labels}: ROI label rows
  \item \code{polygons}: ROI polygon rows
  \item \code{ellipses}: ROI ellipse rows
  \item \code{masks}: ROI mask rows
  \item \code{mask_points}: decoded mask points (or empty data.frame)
}
}
\description{
Reads OME-XML metadata and extracts ROI-linked shape annotations
(labels, polygons, ellipses, and masks). This is a package-safe version
of the exploratory workflow in `dev_polygons.R`.
}
\examples{
\dontrun{
ann <- fetchTiffAnnotations(exampleTiff())
names(ann)
}
}
