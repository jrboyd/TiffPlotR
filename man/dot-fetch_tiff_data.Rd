% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiff_image_fetch.R
\name{.fetch_tiff_data}
\alias{.fetch_tiff_data}
\title{Plot a TIFF image with custom dimensions}
\usage{
.fetch_tiff_data(
  tiff_path,
  x_start,
  x_width,
  y_start,
  y_width,
  resolution = NULL,
  max_pixels = 800,
  precalc_max = NULL,
  show_raw = FALSE,
  quantile_norm = 0.999
)
}
\arguments{
\item{tiff_path}{Path to the TIFF image file}

\item{x_start}{Starting x coordinate (in original image coordinates)}

\item{x_width}{Width of region to read in x direction (in original image coordinates)}

\item{y_start}{Starting y coordinate (in original image coordinates)}

\item{y_width}{Width of region to read in y direction (in original image coordinates)}

\item{resolution}{Resolution level to read from the TIFF file. If NULL, automatically selects resolution to keep image under max_pixels}

\item{max_pixels}{Maximum dimension in pixels for the plotted image. Used for automatic resolution selection}

\item{precalc_max}{Optional data frame with precalculated min/max values per channel for normalization}

\item{show_raw}{If TRUE, displays raw pixel values; if FALSE (default), displays normalized values}

\item{quantile_norm}{Quantile for normalization (default 0.999). Values are divided by this quantile}
}
\value{
A TiffPlotData object containing the sparse image data and a named list of ggplot objects. It also records `tiff_path`, `resolution`, `precalc_max` and `rect` slots.
}
\description{
Reads a region from a TIFF image file and creates a ggplot visualization.
Automatically handles multi-resolution TIFF files and selects appropriate
resolution based on desired pixel dimensions.
}
\examples{
\dontrun{
  .fetch_tiff_data("image.tiff", x_start=100, x_width=400, y_start=200, y_width=400)
}
}
