% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiff_image_fetch.R
\name{select_informative_regions_lowres}
\alias{select_informative_regions_lowres}
\title{Select informative regions using a low-resolution scout pass}
\usage{
select_informative_regions_lowres(
  tiff_path,
  rect_width,
  rect_height,
  n_each = 10,
  scout_resolution = NULL,
  summary_fun = c("mean", "median", "max"),
  channel = NULL,
  seed = NULL,
  output = c("rect", "list")
)
}
\arguments{
\item{tiff_path}{Path to the TIFF image file.}

\item{rect_width}{Candidate region width in full-resolution pixels.}

\item{rect_height}{Candidate region height in full-resolution pixels.}

\item{n_each}{Number of regions to select for each intensity group
(high/mid/low).}

\item{scout_resolution}{Resolution level used for scouting. If NULL,
uses the lowest-resolution pyramid level (largest \code{resolutionLevel}).}

\item{summary_fun}{Summary statistic for region scoring: one of
\code{"mean"}, \code{"median"}, \code{"max"}.}

\item{channel}{Optional channel index to score. If NULL, scores with the
mean across channels.}

\item{seed}{Optional random seed for reproducible tie-breaking.}

\item{output}{Either \code{"rect"} to return only \linkS4class{TiffRect}, or
\code{"list"} to return both rectangles and a scoring table.}
}
\value{
If \code{output = "rect"}, returns a \linkS4class{TiffRect} containing the
selected regions.

If \code{output = "list"}, returns a list with elements:
\itemize{
  \item \code{rect}: selected \linkS4class{TiffRect}
  \item \code{scores}: data.frame with per-tile scores and group labels
}
}
\description{
Uses a single low-resolution image read to score a regular grid of candidate
regions, then selects high-, middle-, and low-intensity rectangles to fetch
later at full resolution.
}
\examples{
\dontrun{
sel <- select_informative_regions_lowres(exampleTiff(), rect_width = 400, rect_height = 400)
obj <- fetchTiffData(exampleTiff(), rect = sel[1])
}
}
