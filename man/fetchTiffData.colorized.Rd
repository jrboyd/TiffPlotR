% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiff_image_fetch.R
\name{fetchTiffData.colorized}
\alias{fetchTiffData.colorized}
\title{Plot a rectangular region of a TIFF image with multi-channel color mapping}
\usage{
fetchTiffData.colorized(
  tiff_path,
  rect = NULL,
  resolution = NULL,
  max_pixels = 800,
  channel_colors,
  channel_names = NULL,
  value_var = "norm_value",
  precalc_max = NULL,
  quantile_norm = 0.999
)
}
\arguments{
\item{tiff_path}{Path to the TIFF image file}

\item{rect}{A \linkS4class{TiffRect} object defining the rectangle region}

\item{resolution}{Resolution level to read from the TIFF file. If NULL, automatically selects resolution}

\item{max_pixels}{Maximum dimension in pixels for the plotted image}

\item{channel_colors}{Channel-to-color mapping. Use an unnamed positional list/vector (index = channel number),
where NULL/NA entries are ignored, or a named list/vector with names that exactly match `channel_names`.}

\item{channel_names}{Optional character vector of channel names. Required for named `channel_colors` mapping.}

\item{value_var}{Variable to use for channel intensities, either "norm_value" or "value" (default "norm_value")}

\item{precalc_max}{Optional data frame with precalculated min/max values per channel for normalization}

\item{quantile_norm}{Quantile for normalization (default 0.999)}
}
\value{
A TiffPlotData object containing the colorized data and a named ggplot object.
}
\description{
Creates a colorized composite visualization from a rectangular TIFF region.
Supports mapping any number of channels to named colors, then blends channel
contributions with additive clamping to RGB.
}
