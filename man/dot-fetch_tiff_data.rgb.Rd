% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiff_image_fetch.R
\name{.fetch_tiff_data.rgb}
\alias{.fetch_tiff_data.rgb}
\title{Plot a TIFF image as RGB with custom dimensions}
\usage{
.fetch_tiff_data.rgb(
  tiff_path,
  rect = NULL,
  resolution = NULL,
  max_pixels = 800,
  red_channel = 6,
  green_channel = 1,
  blue_channel = 5,
  channel_names = NULL,
  value_var = "norm_value",
  precalc_max = NULL,
  quantile_norm = 0.999
)
}
\arguments{
\item{tiff_path}{Path to the TIFF image file}

\item{rect}{A \linkS4class{TiffRect} object defining the rectangle region}

\item{resolution}{Resolution level to read from the TIFF file. If NULL, automatically selects resolution}

\item{max_pixels}{Maximum dimension in pixels for the plotted image}

\item{red_channel}{Channel number to use for red component (default 6)}

\item{green_channel}{Channel number to use for green component (default 1)}

\item{blue_channel}{Channel number to use for blue component (default 5)}

\item{value_var}{Variable to use for RGB values, either "norm_value" or "value" (default "norm_value")}

\item{precalc_max}{Optional data frame with precalculated min/max values per channel for normalization}
}
\value{
A TiffPlotData object containing the RGB data and a named ggplot object. The object includes metadata slots `tiff_path`, `resolution`, `precalc_max`, `rect`, and `img_info`.
}
\description{
Reads a region from a TIFF image and creates an RGB composite visualization.
Combines three specified channels into red, green, and blue components with
automatic quantile normalization and legend display.
}
\examples{
tiff_path = exampleTiff()
.fetch_tiff_data(tiff_path,
  rect = TiffRect(900, 2300, 1400, 2800))
fetchTiffData(tiff_path,
  rect = TiffRect(900,2300,1400,2800))

.fetch_tiff_data.rgb(tiff_path, rect = TiffRect(900, 2300, 1400, 2800), red_channel=6, green_channel=1, blue_channel=5)
fetchTiffData.rgb(tiff_path, rect = TiffRect(900,2300,1400,2800), red_channel=6, green_channel=1, blue_channel=5)
}
