% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiff_image_fetch.R
\name{precalc_max_all_resolutions}
\alias{precalc_max_all_resolutions}
\title{Precompute normalization ranges across all TIFF resolutions}
\usage{
precalc_max_all_resolutions(
  tiff_path,
  min_prob = 0.9,
  max_prob = 0.998,
  min_agg = "min",
  max_agg = "max",
  probs = sort(unique(c(seq(0, 100)/100, 0.99 + seq(1, 9)/1000, min_prob, max_prob))),
  return_by_resolution = FALSE
)
}
\arguments{
\item{tiff_path}{Path to the TIFF image file.}

\item{min_prob}{Quantile used for \code{min_value} (default \code{0.90}).}

\item{max_prob}{Quantile used for \code{max_value} (default \code{0.998}).}

\item{min_agg}{Aggregation function for per-resolution minima.}

\item{max_agg}{Aggregation function for per-resolution maxima.}

\item{probs}{Numeric vector of quantiles to compute up front. Must include
\code{min_prob} and \code{max_prob}.}

\item{return_by_resolution}{If TRUE, return collected quantiles alongside
aggregated output.}
}
\value{
By default, a data.frame with columns \code{channel},
  \code{min_value}, \code{max_value}. If \code{return_by_resolution = TRUE},
  returns a list with elements \code{precalc_max} and \code{quantiles}.
}
\description{
Convenience wrapper around \code{collect_channel_quantiles_all_resolutions()}
and \code{quantiles_to_precalc_max()}.
}
