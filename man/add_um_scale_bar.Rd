% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_bar_um.R
\name{add_um_scale_bar}
\alias{add_um_scale_bar}
\title{Add a micron scale bar to a plot}
\usage{
add_um_scale_bar(
  p,
  unit_per_pixel = NULL,
  unit_name = NULL,
  bar_um = NULL,
  label = NULL,
  position = c("bottomleft", "bottomright", "topleft", "topright"),
  bar_fraction = 0.2,
  pad_fraction = 0.04,
  label_offset_fraction = 0.03,
  line_color = "white",
  text_color = line_color,
  text_size = 3.5,
  line_width = 1,
  xlim = NULL,
  ylim = NULL
)
}
\arguments{
\item{p}{A ggplot object or a \linkS4class{TiffPlotData} object.}

\item{unit_per_pixel}{Microns per pixel (numeric scalar, must be > 0). If
NULL and \code{p} is \code{TiffPlotData}, this is inferred from
\code{p@unit_per_pixel} and \code{p@unit_name} when available.}

\item{bar_um}{Length of the scale bar in microns. If \code{NULL}, a
"nice" value near \code{bar_fraction} of the visible width is chosen.}

\item{label}{Optional custom label. If \code{NULL}, uses
\code{"<bar_um> um"}.}

\item{position}{One of \code{"bottomleft"}, \code{"bottomright"},
\code{"topleft"}, \code{"topright"}.}

\item{bar_fraction}{Target fraction of visible width used when
\code{bar_um = NULL}.}

\item{pad_fraction}{Padding from plot edges, as a fraction of panel width/
height.}

\item{label_offset_fraction}{Vertical offset of the label from the bar, as
a fraction of panel height.}

\item{line_color}{Scale-bar line color.}

\item{text_color}{Label text color.}

\item{text_size}{Label text size.}

\item{line_width}{Scale-bar line width.}

\item{xlim}{Optional numeric length-2 x-range in pixel units. Most users
should leave this as \code{NULL}.}

\item{ylim}{Optional numeric length-2 y-range in pixel units. Most users
should leave this as \code{NULL}.}
}
\value{
A ggplot object, or a \linkS4class{TiffPlotData} object if
  \code{p} is \code{TiffPlotData}.
}
\description{
Adds a horizontal scale bar labeled in microns to a ggplot or to the active
plot inside a \linkS4class{TiffPlotData} object.
}
